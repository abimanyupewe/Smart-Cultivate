import 'package:flutter/material.dart';

class Weather_Monitoring extends StatelessWidget {
  const Weather_Monitoring({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}